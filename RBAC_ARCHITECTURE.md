# RBAC System Architecture

## 🏗️ System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                        BEAUTYSQUAD FRONTEND                          │
│                      (React + TypeScript)                            │
└─────────────────────────────────────────────────────────────────────┘

┌────────────────────┐
│   Authentication   │
│    (Login/Auth)    │
└─────────┬──────────┘
          │ JWT Token + Role
          ▼
┌────────────────────────────────────────────────────────────────────┐
│                                                                    │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │              AuthContext (Global State)                      │ │
│  │  ────────────────────────────────────────────────────────── │ │
│  │  • user: User                                                │ │
│  │  • role: UserRole (Admin | Brand | Influencer)            │ │
│  │  • permissions: Permission[]                               │ │
│  │  • isAuthenticated: boolean                                │ │
│  │  • hasPermission(permission): boolean                      │ │
│  │  • canAccessResource(resource, scope): boolean            │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                    │
│  Consumed by: ALL components, hooks, guards                       │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                                                                    │
│                  RBAC Core System Files                           │
│  ────────────────────────────────────────────────────────────── │
│                                                                    │
│  1. types/roles.ts                                               │
│     • UserRole enum (ADMIN, BRAND, INFLUENCER)                 │
│     • PERMISSIONS object (40+ granular permissions)            │
│     • ROLE_PERMISSIONS map (role → permissions)                │
│     • DATA_ACCESS map (role → data scope)                      │
│     • FEATURE_VISIBILITY map (role → visible features)        │
│     • ACCESSIBLE_DASHBOARDS map (role → accessible routes)   │
│                                                                    │
│  2. utils/roleUtils.ts                                           │
│     • hasPermission(role, permission)                          │
│     • canAccessResource(role, resource, scope)                 │
│     • getRoleColor(role)                                        │
│     • getPrimaryDashboard(role)                                │
│     • [15+ helper functions]                                    │
│                                                                    │
│  3. hooks/usePermission.ts                                        │
│     • usePermission() → check permissions                       │
│     • useRoleInfo() → get role info                            │
│     • useResourceAccess() → check resource access             │
│     • useFeatureAccess() → check feature visibility           │
│     • useCanPerform() → check action capability               │
│     • useDashboardAccess() → check dashboard access           │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                                                                    │
│                   Guards & Display Components                    │
│  ────────────────────────────────────────────────────────────── │
│                                                                    │
│  Guards (PermissionGuard.tsx):                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ <PermissionGuard>    Guard by specific permission       │  │
│  │ <RoleGuard>          Guard by role(s)                   │  │
│  │ <ResourceAccessGuard> Guard by resource access scope    │  │
│  │ <ProtectedRoute>     Guard entire routes                │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                    │
│  Display (RoleBadge.tsx):                                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ <RoleBadge>    Show role with icon/color                │  │
│  │ <RoleInfoCard> Display detailed role info               │  │
│  │ <RoleList>     List roles for selection                 │  │
│  │ <PermissionList> Display role permissions               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                                                                    │
│                     Usage in Components                          │
│  ────────────────────────────────────────────────────────────── │
│                                                                    │
│  Method 1: Using Guard Components                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ <RoleGuard allowedRoles={[UserRole.BRAND]}>            │  │
│  │   <CampaignForm />                                      │  │
│  │ </RoleGuard>                                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                    │
│  Method 2: Using Custom Hooks                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ const { canApproveContent } = useCanPerform();         │  │
│  │ if (canApproveContent) {                               │  │
│  │   return <ApprovalPanel />                             │  │
│  │ }                                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                    │
│  Method 3: Using Utility Functions                              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ if (hasPermission(role, PERMISSIONS.APPROVE_CONTENT)) { │  │
│  │   // Show approval UI                                   │  │
│  │ }                                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                                                                    │
│                    Data Flow & Access Control                    │
│  ────────────────────────────────────────────────────────────── │
│                                                                    │
│       User Role Determination                                    │
│              │                                                    │
│              ▼                                                    │
│       ┌─────────────────┐                                        │
│       │ Get Role from   │                                        │
│       │ JWT Token /     │                                        │
│       │ AuthContext     │                                        │
│       └────────┬────────┘                                        │
│              │                                                    │
│              ▼                                                    │
│       ┌─────────────────────────────────┐                        │
│       │ Look up Role in ROLE_PERMISSIONS │                        │
│       │ → Get available permissions      │                        │
│       └────────┬────────────────────────┘                        │
│              │                                                    │
│              ▼                                                    │
│       ┌─────────────────────────────────┐                        │
│       │ Check DATA_ACCESS for role      │                        │
│       │ → Determine data scope          │                        │
│       │   (all, own, assigned, none)    │                        │
│       └────────┬────────────────────────┘                        │
│              │                                                    │
│              ▼                                                    │
│       ┌─────────────────────────────────┐                        │
│       │ Filter API responses by scope   │                        │
│       │ and permissions                 │                        │
│       └────────┬────────────────────────┘                        │
│              │                                                    │
│              ▼                                                    │
│       ┌─────────────────────────────────┐                        │
│       │ Render UI based on              │                        │
│       │ FEATURE_VISIBILITY              │                        │
│       │ and user permissions            │                        │
│       └─────────────────────────────────┘                        │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                                                                    │
│                   Role-Based Routing Map                         │
│  ────────────────────────────────────────────────────────────── │
│                                                                    │
│  Admin User              Brand User           Influencer User    │
│  ┌──────────────┐       ┌──────────────┐     ┌──────────────┐   │
│  │ / (home)     │       │ / (home)     │     │ / (home)     │   │
│  │ /admin ✓     │       │ /brand ✓     │     │ /influencer ✓│   │
│  │ /brand ✗     │       │ /admin ✗     │     │ /brand ✗     │   │
│  │ /influencer✗ │       │ /influencer✗ │     │ /admin ✗     │   │
│  │ /campaigns ✓ │       │ /campaigns ✓ │     │ /campaigns ✓ │   │
│  │ /influencers✓│       │ /influencers✓│     │ /influencers✓│   │
│  └──────────────┘       └──────────────┘     └──────────────┘   │
│                                                                    │
│  Primary Dashboard Redirect:                                     │
│  Admin     → /admin                                              │
│  Brand     → /brand                                              │
│  Influencer → /influencer                                        │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                                                                    │
│              Approval & Workflow Orchestration                   │
│  ────────────────────────────────────────────────────────────── │
│                                                                    │
│  Campaign Creation Flow:                                          │
│  Brand ──→ Create Campaign → Stored → Ready for Influencers     │
│                                                                    │
│  Content Submission Flow:                                         │
│  Influencer ──→ Draft ──→ Submit ──→ Brand Reviews              │
│                                        │                         │
│                           ┌────────────┴─────────────┐           │
│                           ▼                          ▼           │
│                        Approve                    Reject        │
│                           │                          │           │
│                           ▼                          ▼           │
│                      Published                   Request Edits   │
│                           │                          │           │
│                           └────────────┬─────────────┘           │
│                                       ▼                          │
│                              Audit Log (Admin)                   │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                                                                    │
│                  Backend Integration Points                      │
│  ────────────────────────────────────────────────────────────── │
│                                                                    │
│  1. JWT Token Claims (REQUIRED)                                  │
│     {                                                             │
│       "sub": "user-id",                                           │
│       "role": "admin" | "brand" | "influencer",                 │
│       "email": "user@example.com",                              │
│       "exp": timestamp,                                           │
│       ...                                                         │
│     }                                                             │
│                                                                    │
│  2. API Endpoint Role Validation (REQUIRED)                       │
│     GET /api/campaigns                                           │
│       → Admin: Returns all                                       │
│       → Brand: Returns only own brand's                         │
│       → Influencer: Returns only assigned                       │
│                                                                    │
│  3. API Permission Enforcement (REQUIRED)                         │
│     POST /api/approvals                                          │
│       → Admin: Always allowed                                   │
│       → Brand: Allowed for own campaigns                        │
│       → Influencer: Forbidden (403)                             │
│                                                                    │
│  4. Audit Trail Logging (REQUIRED FOR COMPLIANCE)                │
│     Log all: Create, Edit, Delete, Approve, Reject actions     │
│     Store: User ID, Action, Resource, Timestamp                │
│     Access: Admin only                                           │
│                                                                    │
│  5. Soft Delete Implementation (REQUIRED)                         │
│     Mark records as deleted, don't physically remove             │
│     Only physical delete with Admin confirmation                 │
│     Restore capability for Admin users                           │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                                                                    │
│                  Testing & Verification Points                   │
│  ────────────────────────────────────────────────────────────── │
│                                                                    │
│  Unit Tests (Frontend):                                          │
│  ✓ hasPermission(role, permission)                              │
│  ✓ canAccessResource(role, resource, scope)                     │
│  ✓ getRoleColor(role)                                            │
│  ✓ Guard components render correctly                             │
│  ✓ Hooks return expected values                                  │
│                                                                    │
│  Integration Tests (Frontend):                                   │
│  ✓ Protected routes redirect unauthorized users                 │
│  ✓ AuthContext updates with role                                │
│  ✓ Role-based navigation items show/hide                        │
│  ✓ Permission guards protect sensitive UI                       │
│                                                                    │
│  End-to-End Tests (Full Stack):                                  │
│  ✓ Admin can manage users and view all data                     │
│  ✓ Brand can create campaigns and approve content               │
│  ✓ Influencer can submit content and view own metrics           │
│  ✓ Data isolation verified across roles                          │
│  ✓ Audit logs recorded and accessible                           │
│  ✓ Soft deletes work correctly                                   │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Component Interaction Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    User Component                            │
│                                                              │
│  import { usePermission, useRoleInfo } from "hooks"        │
│  import { PermissionGuard } from "components"              │
│  import { PERMISSIONS } from "types"                       │
│                                                              │
│  function MyComponent() {                                   │
│    const { hasPermission } = usePermission()               │
│    const { isAdmin, isBrand } = useRoleInfo()              │
│                                                              │
│    return (                                                  │
│      <>                                                      │
│        {hasPermission(PERMISSIONS.CREATE_CAMPAIGN) && (      │
│          <CreateBtn />                                       │
│        )}                                                    │
│        <PermissionGuard permission={...}>                   │
│          <ApprovalPanel />                                   │
│        </PermissionGuard>                                    │
│      </>                                                      │
│    )                                                         │
│  }                                                           │
│                                                              │
└────────┬──────────────────────────────────────────────────────┘
         │ uses
         ↓
┌─────────────────────────────────────────────────────────────┐
│                  AuthContext (useAuth)                      │
│                                                              │
│  Provides:                                                  │
│  • role: UserRole                                           │
│  • permissions: Permission[]                               │
│  • hasPermission(): boolean                                 │
│  • canAccessResource(): boolean                            │
│                                                              │
└────────┬──────────────────────────────────────────────────────┘
         │ reads from
         ↓
┌─────────────────────────────────────────────────────────────┐
│           types/roles.ts (Definitions)                      │
│                                                              │
│  ROLE_PERMISSIONS[role]                                     │
│  → Permission[]                                              │
│                                                              │
│  DATA_ACCESS[role]                                          │
│  → DataAccessScope                                           │
│                                                              │
│  FEATURE_VISIBILITY[role]                                   │
│  → FeatureVisibility                                         │
│                                                              │
└────────────────────────────────────────────────────────────────┘
         ↑
         │ used by
┌────────┴──────────────────────────────────────────────────────┐
│       utils/roleUtils.ts (Logic Functions)                  │
│                                                              │
│  hasPermission(role, permission)                            │
│    → Check ROLE_PERMISSIONS[role]                           │
│                                                              │
│  canAccessResource(role, resource, scope)                   │
│    → Check DATA_ACCESS[role][resource]                      │
│                                                              │
│  isFeatureVisible(role, feature)                            │
│    → Check FEATURE_VISIBILITY[role][feature]               │
│                                                              │
└──────────────────────────────────────────────────────────────┘
         ↑
         │ used by
┌────────┴──────────────────────────────────────────────────────┐
│         hooks/usePermission.ts (React Hooks)               │
│                                                              │
│  usePermission()                                             │
│    → wraps hasPermission()                                   │
│                                                              │
│  useResourceAccess()                                         │
│    → wraps canAccessResource()                              │
│                                                              │
│  useRoleInfo()                                               │
│    → wraps role metadata                                     │
│                                                              │
└────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

```
User Logs In
    ↓
Backend validates credentials
    ↓
Backend returns JWT with role claim
    ↓
Frontend stores JWT in localStorage
    ↓
AuthContext.login() called with:
  • User data
  • JWT token
    ↓
AuthContext extracts role from User object
    ↓
AuthContext looks up ROLE_PERMISSIONS[role]
    ↓
AuthContext creates permissions array
    ↓
Component calls useAuth()
    ↓
Gets role, permissions, hasPermission(), canAccessResource()
    ↓
Component renders based on:
  ├─ Guards (RoleGuard, PermissionGuard)
  ├─ Conditional rendering with hooks
  └─ Feature visibility checks
    ↓
User interacts with allowed features
    ↓
All API requests include JWT token
    ↓
Backend validates:
  ├─ Token is valid & not expired
  ├─ Role has required permission
  └─ User can access requested resource
    ↓
Backend returns filtered data by role
    ↓
Frontend updates UI
    ↓
Audit log recorded (backend)
```

---

## 🎯 Role Responsibility Matrix

```
                Admin        Brand        Influencer
              ─────────────────────────────────────
User Mgmt      OWNER        —            —
Campaigns      VIEW ALL     OWNER(own)   VIEW(assigned)
Content        REVIEW       OWNER(own)   OWNER(own)
Approvals      VIEW ALL     OWNER        —
Metrics        VIEW ALL     OWNER(own)   OWNER(own)
Audit Log      FULL         —            —
Settings       FULL         —            —
```

---

This architecture ensures:
- ✅ Complete separation of concerns
- ✅ Type-safe permission checking
- ✅ Reusable components and hooks
- ✅ Flexible permission system
- ✅ Clear data flow
- ✅ Easy to test and maintain
